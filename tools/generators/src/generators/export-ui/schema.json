{
  "$schema": "http://json-schema.org/schema",
  "$id": "ExportUi",
  "title": "Export UI components",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project to export UI components from",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["project"]
}
